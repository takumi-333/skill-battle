# AGENTS.md

## このプロジェクトの概要
Godotを用いたゲーム開発

## ドキュメント運用

### `docs/ideas/`

- アイデア、検討メモ、試作案など、まだ仕様として確定していない内容を保存する。
- 内容は変更・撤回・削除してよい。ここにある記述だけを根拠に実装しない。
- 実装することが決まった内容は、必要に応じて `docs/specs/` へ整理して移す。

### `docs/specs/`

- 実装対象として確定した仕様を保存する。
- 実装は原則として、このディレクトリ内の仕様に従う。
- 仕様と実装に差異がある場合は、実装を変更する前に仕様の意図を確認し、必要なら仕様を更新する。

## スペック駆動開発の基本原則

### 基本フロー

1. **ドキュメント作成**: 永続ドキュメント(`docs/specs/`)で「何を作るか」を定義
2. **作業計画**: ステアリングファイル(`docs/.steering/`)で「今回何をするか」を計画
3. **実装**: tasklist.mdに従って実装し、進捗を随時更新
4. **検証**: テストと動作確認
5. **更新**: 必要に応じてドキュメント更新

### 重要なルール

#### ドキュメント作成時

**1ファイルずつ作成し、必ずユーザーの承認を得てから次に進む**

承認待ちの際は、明確に伝える:
```
「[ドキュメント名]の作成が完了しました。内容を確認してください。
承認いただけたら次のドキュメントに進みます。」
```
#### ステアリングファイル管理

作業ごとに `docs/.steering/[YYYYMMDD]-[タスク名]/` を作成:

- `requirements.md`: 今回の要求内容
- `design.md`: 実装アプローチ
- `tasklist.md`: 具体的なタスクリスト

命名規則: `20250115-add-user-profile` 形式

#### ステアリングファイルの管理

**作業計画・実装・検証時は`steering`スキルを使用してください。**

- **作業計画時**: `Skill('steering')`でモード1(ステアリングファイル作成)
- **実装時**: `Skill('steering')`でモード2(実装とtasklist.md更新管理)
- **検証時**: `Skill('steering')`でモード3(振り返り)

な手順と更新管理のルールはsteeringスキル内に定義されています。」