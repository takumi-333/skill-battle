# タスクリスト

- [x] 専用サーバー課題スナップショットで、同一課題IDのローカル筆跡を保持するよう修正する。
- [x] 同一課題IDでの筆跡保持と新規課題IDでの初期化を検証する自動テストを追加する。
- [x] Godotヘッドレステストとプロジェクト起動確認を実行する。
- [x] 検証結果を記録し、ステアリングを振り返る。

## 検証結果

- `godot --headless --path . --script res://tests/network/lobby_ready_ui_test.gd`: 成功。追加した、同一課題IDでのローカル筆跡保持と新規課題IDでの初期化を確認。
- `godot --headless --path . --script res://tests/network/match_simulation_test.gd`: 成功。サーバー権威の詠唱士なぞり採点を含む既存テストが成功。
- `godot --headless --path . --quit-after 3`: 成功。起動時のスクリプトエラーなし。
- 上記の実行では `user://logs` の書き込みとWindows証明書ストアの環境警告が出たが、いずれも終了コードは0で、テスト結果への影響はなかった。

## 振り返り

- 未提出の入力はクライアントの一時状態として扱い、同じ課題IDの権威スナップショットで上書きしない。
- 課題IDの切り替わりを初期化境界にすることで、次の課題へ前回の筆跡を持ち越さない。
