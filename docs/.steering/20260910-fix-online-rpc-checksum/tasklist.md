# タスクリスト

## フェーズ1: 原因確認

- [x] 貼り付けログから RPC checksum と `submit_match_input` の引数型ずれを確認する。
- [x] クライアントと専用サーバーの RPC 定義を比較する。

## フェーズ2: 修正

- [x] 専用サーバー側に不足している受信専用 RPC の空実装を追加する。
- [x] RPC 定義の一致を確認し、既存の処理を変更しないことを確認する。

## フェーズ3: 検証

- [x] ネットワークシミュレーションテストを実行する。
- [x] Godot ヘッドレス起動を実行する。

## 実装後の振り返り

### 実装完了日

2026-09-10

### 計画と実績の差分

- RPCの不足だけでなく宣言順も揃え、GodotのRPCメソッドIDずれを防止した。

### 学んだこと

- 同一ノードパスをクライアントと専用サーバーで共有する場合、RPC定義の集合と順序を一致させる必要がある。

### 次回への改善提案

- RPCを追加する際はクライアントとサーバーの両方に同じ順序で契約を追加し、接続テストを先に実行する。
- ローカル専用サーバーとのENet接続で checksum エラーが発生しないことを確認した。プローブは予約情報なしのため、接続後に想定どおり入室情報エラーで終了した。
- 初回スナップショットのMTU超過警告は残るが、RPC checksum／引数型エラーは発生しないことを確認した。
