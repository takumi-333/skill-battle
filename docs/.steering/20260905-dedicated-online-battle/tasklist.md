# タスクリスト

- [x] サーバー権威の共有プロトコル、シミュレーション、ルームセッションを実装する。
- [x] Dedicated Server のヘッドレス入口と ENet RPC を実装する。
- [x] ロビー API、SQLite、短命トークンとテストを実装する。
- [x] クライアント接続用の API/ENet アダプターと公開ルーム一覧 UI を追加する。
- [x] Linux/WSL2/OCI の実行・配備設定を追加する。
- [x] 静的検証、トークン／DB テスト、Godot ヘッドレス起動を実行する。

## 実装後の振り返り

### 実装完了日

2026-09-05

### 検証結果と差分

- Godot 4.7.2 で Dedicated Server シーンおよび通常のメインシーンをヘッドレス起動し、追加 GDScript のパースを確認した。
- Python 環境に pytest は未導入だったため、同じテスト関数を直接実行し、トークンの room/slot/期限検証と SQLite の 2 席予約上限を確認した。
- WSL2、OCI VM、TLS、ARM64、外部ネットワーク 2 台、20 接続の負荷試験は実行環境・資格情報がないため未実施。`deploy/deploy-server.md` に実行手順と確認境界を記録した。
- 初期実装では共有シミュレーションに移動・通常攻撃・HP・時間切れを実装した。既存の課題スキルの全サーバー権威化は、同じ `MatchSimulation` 境界へ移す次の実装単位として残る。
