# タスクリスト

- [x] 補間処理と既存テストの構成を確認し、ID索引方式を実装する。
- [x] 順序が変化してもIDによる補間が維持される回帰テストを追加する。
- [x] 関連するネットワークテストを実行し、Godotのヘッドレス起動でエラーを確認する。
- [x] 検証結果を記録し、未実施項目がないことを確認する。

## 検証結果

- 2026-09-11: 実機での改善が確認できなかったため、ユーザー判断によりこの方式のコード変更を安全に巻き戻した。代替として `20260911-event-driven-izayoi-volley` でイベント駆動の描画同期を試す。

- 2026-09-11: `godot --headless --path . --script res://tests/network/match_simulation_test.gd` が成功。ID順序を変えた投射物の補間と、IDなしエンティティのフォールバックを含むサーバー権威テストが通過した。
- 2026-09-11: `godot --headless --path . --quit-after 3` が成功。プロジェクトの起動時スクリプトエラーは発生しなかった。
- 上記の実行では、既存環境により `user://logs` の書き込みおよびWindows証明書ストアに関する警告が出たが、いずれも終了コード0で、本変更に関するエラーはない。
- `git diff --check` は対象ファイルで空白エラーなし。
