# 設計

- 各周の `shot` (0〜15) ごとに、時計回り・反時計回りの2投射物へ同じ遅延値 `((cycle * 16) + shot) * 0.08` を設定する。
- 時計回りは真上から角度を増加、反時計回りは真下から角度を減少させる。既存の `fixed_direction` を保持し、発射直前の発動者位置だけを取得する。
- クールダウン設定は汎用スキル2の値を変えず、詠唱士のスキル2だけを6秒にする。
- テストは同一遅延値の対となる投射物、開始方向、更新後の周回数・総弾数、詠唱士だけのクールダウンを検証する。
