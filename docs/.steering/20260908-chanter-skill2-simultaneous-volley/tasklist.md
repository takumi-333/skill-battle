# タスクリスト

- [x] 更新済みの仕様書と現行のスキル2発射・クールダウン経路を確認し、同時射撃の改修方針を記録する。
- [x] ローカル対戦とサーバー権威シミュレーションを、時計回り・反時計回りの同時射撃と新しい周回数へ更新する。
- [x] 詠唱士スキル2だけの6秒クールダウンを実装し、仕様書表現を確認する。
- [x] 同時発射・周回数・クールダウンを検証する自動テストを更新する。
- [x] Godotテストとヘッドレス起動を実行し、結果と振り返りを記録する。

## 検証結果

- `godot --headless --path . --script res://tests/network/match_simulation_test.gd`: 成功。同時発射ペア、スコア境界ごとの1〜4周、開始方向、固定方向、移動後の発射位置、ダメージ、6秒クールダウンを検証。
- `godot --headless --path . --script res://tests/network/lobby_ready_ui_test.gd`: 成功。
- `godot --headless --path . --quit-after 3`: 成功。スクリプトのパースエラーなし。
- 各コマンドで `user://logs` と Windows 証明書ストアに関する環境警告が出たが、終了コードは0だった。

## 振り返り

- 2列を連続で予約する方式ではなく、1発射時刻に時計回り・反時計回りの弾を対で予約することで、同時性をゲーム状態にも明示した。
- 最大投射物数は128発へ減少したが、既存の192発上限は安全な余裕として維持する。
