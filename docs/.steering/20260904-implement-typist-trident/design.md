# 設計

- スキルごとの課題時間は `ChallengeDefinition.time_limit_seconds` を唯一の参照元として設定し、実際のタイマー判定も定義値から取得する。
- 三叉震槌発動時に専用の一時演出状態を生成する。ハンマーの叩き付け、発動地点の衝撃リング、地面の亀裂風ライン、5方向の衝撃波を既存の `_draw` ワールド描画へ追加する。
- 画面揺れは既存の `trigger_screen_shake` を使い、波動命中時には追加しない。
- 演出状態はホストのゲーム状態へ含め、クライアントでも同じ見た目を再生できるようにする。
