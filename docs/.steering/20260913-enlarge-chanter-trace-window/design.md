# 設計

- `match_prototype.gd` の詠唱士用レイアウトを 920x540 にし、画面中央の `(180, 90)` に配置する。
- 詠唱士のときだけ `TraceCanvas` の最小高を 360px にして、なぞり領域を広げる。
- なぞり図形の中心を `(460, 180)` に移し、各半径を約 1.3 倍へ拡大する。
- `match_simulation.gd` の図形定義にも同じ定数を反映し、送信された軌跡をサーバーが正しい形状で採点できるようにする。
- 図形位置・サイズを確認する既存のネットワークテストは新しい基準値に更新する。
