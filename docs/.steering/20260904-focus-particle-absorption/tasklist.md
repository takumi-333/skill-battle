# タスクリスト

- [x] 既存の集中円エフェクトと描画順を確認する
- [x] キャラクター別パーティクルテクスチャの読込処理を追加する
- [x] 周囲から中心へ移動・吸収されるパーティクル描画を実装する
- [x] 既存の集中円を削除する
- [x] Godotを実行して表示とエラーを確認する
- [x] 振り返りを記録する

## 実装後の振り返り

実装完了日：2026-09-04

- 既存の集中時の円形 `draw_arc` を削除した。
- `assets/effects/focus_particles/` のキャラクター別PNGを動的に読み込み、集中中だけ描画するようにした。
- 粒子は疑似ランダムな周囲位置からキャラクターのヒットボックス中心へ移動し、中心で透明になって吸収される。
- Godot headless editor起動で構文エラーがないことを確認し、通常のheadless起動でも `ready complete` まで到達した。
- 環境依存の証明書ストア警告と `user://logs` 保存エラーは出たが、今回の実装に起因するエラーではない。
