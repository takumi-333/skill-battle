# タスクリスト

## フェーズ1: 生成準備

- [x] 仕様・アートディレクション・既存UIコンセプトを確認する
- [x] タイトルロゴ、主ボタン、副ボタンの生成仕様を確定する

## フェーズ2: アセット作成

- [x] タイトルロゴを生成し、内容を確認する（既存コンセプトに合わせ、表記は `SKILL BATTLERS` とする）
- [x] 主ボタンを生成し、内容を確認する
- [x] 副ボタンを生成し、内容を確認する
- [x] 3アセットを `assets/ui/` に配置する

## フェーズ3: 検証と台帳更新

- [x] PNGの寸法と透明度を検査する
- [x] UIアセット台帳の配置先・規格・状況を更新する
- [x] Markdownの差分チェックを実行する

## 実装後の振り返り

### 実装完了日

2026-08-31

### 計画と実績の差分

- 画面への組み込みはスコープ外とし、タイトルロゴとボタン背景のPNG作成・台帳登録までを完了した。
- 生成PNGは実装側で縮尺を指定して使えるよう、高解像度の透過原寸を維持した。

### 学んだこと

- 画像生成時は文字なしボタン素材として指定すると、ゲーム内でローカライズ可能なラベルを重ねられる。
- アセット台帳に原寸と画面表示基準を併記すると、素材制作とUI実装の責務を分離できる。

### 次回への改善提案

- 状態別の見た目が必要になった段階で、ホバー・押下・無効の各状態を同一の命名規則で追加する。
