# 3キャラクター・ドット絵8方向スプライト実装 — タスクリスト

## フェーズ1: 生成準備

- [x] 既存の打鍵者試験シートと、大判アニメ調素材の参照箇所を確認する
- [x] 算術士・詠唱者のキャラクター契約と64px・8方向グリッドを生成プロンプトへ整理する

## フェーズ2: ドット絵素材の作成

- [x] 算術士の8方向・40フレームドット絵シートを生成・確認する
- [x] 詠唱者の8方向・40フレームドット絵シートを生成・確認する
- [x] 3シートの背景を透過化し、512×320px RGBAへ正規化する
- [x] 打鍵者試験シートを最終ファイル名へ採用し、3シートを `assets/characters/` へ配置する

## フェーズ3: Godotへの反映

- [x] 大判アニメ調テクスチャの読み込みを3キャラクターの8方向シートへ置き換える
- [x] `facing` を8方向の列へ変換し、停止・4フレーム歩行のソース矩形を実装する
- [x] 左右反転と大判素材向けの表示位置を廃止し、64pxスプライトとHUD位置へ調整する
- [x] 能力・課題ロジックの内部IDと表示用IDが分離されたままであることを確認する

## フェーズ4: 検証

- [x] 3シートの寸法、RGBA透過、40セルを検査する
- [x] Godotの静的チェックまたは起動チェックを実行する（`godot` / `godot4` 実行ファイルが環境にないため、起動の代わりにスプライト参照・8方向対応・差分を静的検査）
- [x] 3キャラクターの停止・上下・左右・斜め移動の表示を確認する（起動環境なしのため、`facing` の45度区分、列対応、停止・歩行行の切替を静的確認）
- [x] スキル・課題・対戦ルールに変更がないことを確認する（`character_id` の分岐を維持し、描画は `visual_id` のみを参照することを確認）

## 実装後の振り返り

- 完了日: 2026-08-29
- 実績: 打鍵者・算術士・詠唱者の64px・8方向・停止＋4フレーム歩行のドット絵シートを追加し、対戦画面のキャラクター描画を8方向スプライトへ置き換えた。
- 生成・検査で調整した点: 算術士・詠唱者の生成元シートはセルが縦長だったため、元セルごとに縦横比を保って64×64pxセルの下端へ配置した。打鍵者の試験シートは最終ファイル名へ採用した。3シートすべてのRGBA・512×320px・40セルを検査した。
- 次回への改善: Godot実行環境で、実際の8方向移動・HUDとの重なり・歩行速度を確認し、必要なら64pxセル内の表示サイズを調整する。

## 修正: 定数式のパースエラー

- [x] `PackedInt32Array` の定数式エラーを、方向別の `match` 分岐へ置き換える
