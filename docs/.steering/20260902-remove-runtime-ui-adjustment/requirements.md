# 要求

- 実行中UI調整ツールをプロジェクトから廃止する。
- 起動中にUIの位置・サイズを変更したり、設定ファイルへ保存したりする経路を残さない。
- 既存の通常UIと入力処理は維持する。
- 廃止後に不要となる調整結果ファイルと専用の削除手順書を取り除く。

# 完了条件

- `scripts/match_prototype.gd` にUI調整モード、ショートカット、保存・復元処理が存在しない。
- `resources/ui_layout_overrides.cfg` が存在しない。
- 通常のGodot実行でスクリプトエラーが発生しない。
