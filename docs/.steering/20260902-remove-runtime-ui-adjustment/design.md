# 設計

`MatchPrototype` に埋め込まれている実行中UI調整機能を削除する。UIの正式な位置・サイズは、各UI生成処理と `MenuLayoutData` が定義する値だけを使用する。

削除対象は、オーバーレイ、対象ノードの収集、位置・サイズの保存・復元、F9/F10等の入力処理、調整専用の状態変数である。調整結果ファイルは廃止し、既存の削除手順書も不要になるため削除する。

通常入力は、これまでUI調整入力を先に処理していた `_input()` から直接既存のゲーム入力へ流す。
