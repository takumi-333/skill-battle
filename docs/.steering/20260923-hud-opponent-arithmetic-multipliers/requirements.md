# 要求

- 相手が算術士のときだけ、その相手の `f(x) = x` または倍率付き表示を当該HPバーの直下に表示する。
- `duel` は1本、`free_for_all` は相手1・相手2の各HPバーに対応する独立した表示を持つ。
- 非算術士の相手には該当表示を出さない。
- 自分用の算術士倍率表示とHPバーの既存表示は変えない。

# 受け入れ条件

- duelで相手が算術士なら、相手HPバー直下に表示される。
- FFAで2人の相手が算術士なら、各HPバー直下に別々の倍率が表示される。
- FFAで片方のみ算術士なら、もう片方の表示は隠れる。
