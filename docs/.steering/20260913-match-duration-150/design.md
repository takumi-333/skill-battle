# 設計

- ローカル対戦の `MatchState` と `MatchPrototype` の試合時間定数を150秒へ変更する。
- Dedicated Serverの `MatchSimulation` の試合時間定数を同じ値へ変更し、開始snapshotと時間切れ判定を一致させる。
- 設定Resource、リザルト画面の初期文言、ネットワークテスト、関連仕様を150秒（02:30）に更新する。
