# タスクリスト

- [x] 既存の未解放用立ち絵と選択UIの構成を確認する。
- [x] 未解放3枠を指定キャラクターのシルエット・`？？？` 表示へ更新し、通常枠と同じレイアウトにする。
- [x] Godotのシーン検証とヘッドレス起動でエラーがないことを確認する。
- [x] 実装内容と検証結果を振り返りへ記録する。

## 実装後の振り返り

- 未解放3枠を狙撃士・拍律士・構築士の専用立ち絵に対応付け、既存のシルエット用シェーダーで黒一色に変換した。
- 各枠を `HBoxContainer` 内のシルエットと `？？？` ラベルで構成し、画像と名前の間隔を 8 px に統一した。個別 `TextureRect` とボタン標準テキストが競合していた構成を解消した。
- `godot --headless --path . --quit-after 3` で起動し、`character_screen.tscn` の解析エラー・スクリプトエラーがないことを確認した。初回エディタ起動時に新規 PNG のインポート前の一時的な参照エラーが出たが、インポート完了後の再実行では解消している。
