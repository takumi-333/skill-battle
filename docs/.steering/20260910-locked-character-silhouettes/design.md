# 設計

- 狙撃士は仕様の外見（長銃・義手・顔を隠すスカーフ）に沿って生成した `sniper_idle.png` を使う。拍律士・構築士は既存の `rhythmer_idle.png` と `engineer_idle.png` を再利用し、シーン内の既存シルエット用シェーダーで黒一色に変換する。
- 通常キャラクターと同様に `Button.icon` を使う。これによりアイコンとラベルの標準レイアウトを共有し、個別 `TextureRect` による余白の詰まりを解消する。
- 未解放枠のボタンは `disabled` とし、既存の操作不能状態を維持する。
