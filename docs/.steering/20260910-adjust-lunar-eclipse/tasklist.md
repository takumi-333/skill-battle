# タスクリスト

## フェーズ1: ルール更新

- [x] ブラックホール吸引を中心到達型へ更新し、時間定数を2秒／2.5秒へ変更する。
- [x] レーザーの判定線分を発射地点からマップ端までに更新する。

## フェーズ2: 演出更新

- [x] `chanter_beam.png` を横向きに描画し、溜め中も含めた2秒・2.5秒のタイムラインへ更新する。

## フェーズ3: 検証

- [x] 月蝕の自動テストを新しい時間・吸引終端・レーザー状態に合わせて更新する。
- [x] Godotネットワークテストとヘッドレス起動を実行する。

## 実装後の振り返り

### 実装完了日

2026-09-10

- レーザー長の上限共有を修正し、射出地点からマップ端まで描画することを確認。射出中の二重描画も除去し、`chanter_beam.png` 1枚で表現する。
- 元画像の白い芯を紫色へ乗算していた描画色を `Color.WHITE` に戻し、素材本来の色を保持する。
- 色変調そのものを取り除き、`chanter_beam.png` をデフォルト描画する。
- 発射地点から100pxまでを24px→300pxのテーパー形状で描画し、以降は最大幅を維持する。中心線が曲がらないよう短い帯へ分割する。

### 計画と実績の差分

- 溜め時間と射出時間を別定数に分離し、判定と描画のタイムラインを同じ状態値から算出した。
- 縦長の既存ビーム素材は、描画変換で90度回転して横長のレーザーとして再利用した。

### 学んだこと

- ブラックホールは視覚的な中心と吸引処理だけを持ち、独立した攻撃判定を追加しない構成を維持した。

### 次回への改善提案

- 持続時間を変更する場合は、終了時刻だけでなくダメージ発生時刻とテストの経過時間も同時に更新する。
