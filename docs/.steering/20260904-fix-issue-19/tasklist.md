# タスクリスト

- [x] 課題種別判定を共通ヘルパーへ整理し、鍵片追弾の入力不具合を修正する
- [x] 待機画面の相手枠へ人影画像を常時表示する
- [x] クールダウン進行を背景マテリアルへ反映する
- [x] Godot ヘッドレス起動と関連テストで検証する

## 実装後の振り返り

- 実装完了日: 2026-09-04
- 課題種別を `challenge_definition.challenge_type` 優先の共通判定へ整理し、ネットワーク受信側のフォールバックも追加した。これにより `big_typing_keycap_ii` がタイピング入力として処理される。
- 待機画面の相手枠は接続状態に関わらず人影画像を表示し、接続有無はステータスアイコンで表現する。
- スキルアイコンと背景の両マテリアルへ同じクールダウン進行値を渡し、背景も放射状に明暗変化するようにした。フォールバック生成経路も同じ設定へ統一した。
- 検証: `godot --headless --path . --quit-after 3` は成功し、起動ログにエラーはなかった。`pytest` は環境にコマンドがなく実行できなかった。
- 今後の改善: 実機または2クライアントで、鍵片追弾の再入力、待機画面、クールダウン表示を目視確認する。
