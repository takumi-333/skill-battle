# タスクリスト

- [x] 安定キー生成を追加
- [x] Git管理対象への保存・復元を追加
- [x] 旧user://データからの移行を追加
- [x] 操作案内と削除手順書を更新
- [x] Godot構文チェックと起動確認

## 実装後の振り返り

保存キーを親画面・Control種別・内容・初期位置・サイズから構成し、UI生成順に依存しないようにした。調整値は `res://resources/ui_layout_overrides.cfg` に保存し、旧 `user://` ファイルから可能な範囲で初回移行する。Godot 4.7.2で構文チェックと起動確認を実施した。
