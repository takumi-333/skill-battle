# タスクリスト

## フェーズ1: 調査と移動

- [x] 実行用キャラクター画像とGodot参照を特定する
- [x] 待機立ち絵とスプライトシートを用途別ディレクトリへ移動する
- [x] Godot参照とimportメタデータのパスを更新する

## フェーズ2: 検証と振り返り

- [x] 残存する旧パスがないことを確認する
- [x] Godotをheadless起動して素材読込を確認する
- [x] tasklistの全項目を確認し、振り返りを記録する

## 実装後の振り返り

実装完了日: 2026-09-01

計画との差分: 旧版を保持する `assets/characters/old/` は `.gdignore` によりGodotのスキャン対象外であり、実行用の6素材だけを移動した。

学んだこと: PNGと対応する `.import` を同時に移動し、`source_file` を更新すれば、Godotは同じUIDを保ったまま新しいパスへ再インポートできる。

次回への改善提案: 新規のキャラクター立ち絵は `portraits/`、戦闘用シートは `sprites/` を採用先として指定する。
