# タスクリスト

## フェーズ1: 調査と計画

- [x] 黄金時間IIが通信バリデーションで拒否される箇所を確認する
- [x] 黄金時間Iが再戦復元で失われる箇所を確認する

## フェーズ2: 実装

- [x] loadout通信の黄金時間II許可リストを修正する（`typist_golden_time_ii`を許可）
- [x] 再戦時の黄金時間I復元を修正する（`typist_golden_time_i`を保存）
- [x] 再発防止テストを追加する（通信受理・状態反映・再戦復元）

## フェーズ3: 検証

- [x] Godot headlessネットワークテストを実行する（成功）
- [x] Godotプロジェクトのheadless起動確認を実行する（成功、環境由来の警告あり）
- [x] 作業ツリーと変更範囲を確認する

## 実装後の振り返り

### 実施日

2026-09-10

### 結果

- `godot --headless --path . --script res://tests/network/match_simulation_test.gd` は成功し、既存テストと追加テストが通過した。
- `godot --headless --path . --editor --quit` と `godot --headless --path . --quit-after 2` は終了コード0だった。
- ログ保存先 `user://logs` と証明書ストア、エディター設定保存について環境由来の警告が出たが、今回のコードエラーは発生しなかった。

### 学び

- 新しいスキルIDを追加する際は、クライアント送信、通信バリデーション、サーバー復元、再戦復元を同時に確認する必要がある。

### 次回への改善

- 新スキル追加時にロードアウト通信契約のテストを必須化する。
