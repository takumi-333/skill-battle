# タイトル画面修正 タスクリスト

## フェーズ1: タイトル画面状態の追加

- [x] `match_prototype.gd`にロゴ演出経過時間、演出時間、開始プロンプト参照を追加する
- [x] タイトル画面へ入る際にロゴ演出経過時間とプロンプト表示状態を初期化する

## フェーズ2: ロゴ表示演出

- [x] タイトル画面の`_process(delta)`でロゴ演出経過時間を更新する
- [x] `_draw()`のロゴ描画を左から右へのクリッピング表示に変更する
- [x] ロゴの固定位置・サイズを維持し、演出完了後に全幅表示する

## フェーズ3: 開始プロンプト演出

- [x] 開始プロンプトをメンバー変数として保持する
- [x] タイトル画面中の経過時間に応じてプロンプトの透明度を周期的に変更する
- [x] タイトル画面以外ではプロンプトの表示状態をリセットする

## フェーズ4: 開始音と入力処理

- [x] タイトル画面のSpace・Enter・左クリック入力で既存のUI音を再生する
- [x] 開始音が鳴った後にホーム画面へ遷移することを確認する
- [x] タイトル画面以外の入力で開始音が鳴らないことを確認する

## フェーズ5: 検証

- [ ] Godotを実行し、タイトル画面のロゴ演出とプロンプト点滅を目視確認する（ヘッドレス環境のため未確認）
- [ ] Space・Enter・左クリックの開始音とホーム画面遷移を確認する（ヘッドレス環境のため未確認）
- [x] Godotのスクリプトパースエラーがないことを確認する（環境側の証明書・ログ書き込みエラーは残存）
- [x] 実装結果と検証結果を「実装完了後の振り返り」に記録する

## 実装完了後の振り返り

- 実装完了日: 2026-08-30
- 設計と実装の差分: Godot 4.7.2に`draw_set_clip_rect()`が存在しなかったため、`draw_texture_rect_region()`でロゴ画像の左側ソース領域を段階的に描画する方式へ変更した。
- 検証結果: `godot --headless --path . --editor --quit`でスクリプトのパースエラーがないことを確認。`godot --headless --path . --quit-after 3`もゲームコードのエラーなく起動した。環境側で証明書ストア警告と`user://logs`書き込みエラーが発生した。
- 次回への改善点: GUI実行環境でロゴの表示タイミング、プロンプトの明滅、3種類の入力音を実機目視・聴取確認する。
