# タスクリスト

## フェーズ1: GitHub Issue入力の整備

- [x] Steeringファイルと既存ガイドを確認する
- [x] 開発依頼用Issue Formとラベル定義を追加する

## フェーズ2: 同期の自動化

- [x] GitHubラベルを適用するPowerShellスクリプトを追加する
- [x] `ready-for-agent` 用のIssue同期ワークフローを追加する

## フェーズ3: 検証と文書化

- [x] ガイドへ第1段階の具体的な利用手順を追記する
- [x] YAML・PowerShell・構成を静的検査する
- [x] 実装後の振り返りを記録する

---

## 実装後の振り返り

### 実装完了日

2026-08-30

### 計画と実績の差分

- GitHubのデフォルトブランチがローカルでは `master` だったため、ガイドとワークフローは固定の `main` ではなくGitHub APIが返すデフォルトブランチを使う実装に変更した。
- Codexの実行認証やself-hosted runnerは未導入のため、ワークフローはIssue同期PRの作成までに限定した。

### 学んだこと

- PowerShellスクリプトはUTF-8 BOMなしの環境でも解析できるよう、実行時のメッセージをASCIIにした。
- JSONはPowerShellで標準解析できるため、ラベル定義の機械可読な正本として採用した。

### 次回への改善提案

- YAMLパーサーを備えたPython実行環境は利用できなかった。GitHubへpush後にActions画面でワークフローの構文・実行を確認する。
- 第2段階では、同期済みIssueと同じブランチを使ってCodexを実行するワークフローを追加する。
