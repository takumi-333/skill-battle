# 設計

キャラクター画面の候補ボタンは `MatchPrototype.skill_candidates_for()` が返す文字列を使用する。この関数のスキル3分岐では、算術士と詠唱士の先頭候補が実装済みでも「未実装」を返している。

ロードアウトと発動処理で使用しているスキルIDを確認し、スキル仕様書の名称に合わせて、各キャラクターのスキル3の先頭候補だけを実装済み名称へ置き換える。候補の選択可否は既存の「未実装」文字列判定をそのまま利用する。

画面レイアウトやノード構成は変更しない。
