# 要件

- 公開Lobbyを試合終了時に閉じても、接続済みの2人は結果画面で再戦またはロビー復帰を選べるようにする。
- `start-local.ps1`で起動する開発用Lobbyへ、ソースから起動するデバッグ版クライアントが追加の手入力なしで接続できるようにする。配布用releaseへローカルURL・tokenを含めない。
- Dedicated Serverの起動時に、前回プロセスが残した接続中・対戦中のLobby状態を安全に終了し、幽霊ルームを公開しない。
- Funnelの公開先検査を、Gateway一件だけが`http://127.0.0.1:8080`へ向くことを厳密に確認する実装にする。
- API応答と公開手順書・アーキテクチャ仕様の不一致を修正する。
- 上記の回帰を自動テストへ追加し、Python、Godot、PowerShell構文、ヘッドレス起動で検証する。
