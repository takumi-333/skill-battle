# タスクリスト

## フェーズ1: 変更影響の確認

- [x] 変更差分と既存の接続・起動契約を比較し、互換性リスクを特定する。

## フェーズ2: 回帰検証

- [x] PythonのLobby・Gateway・token・databaseテストを実行する。
- [x] Godotのネットワーク・UIテストとヘッドレス起動を実行する。
- [x] 生成済みWindowsクライアントをヘッドレス起動し、配布物が起動可能か確認する。
- [x] Windows PowerShellスクリプトの構文と安全なローカル起動前提を検証する。

## フェーズ3: 報告

- [x] 検出事項、確認済みの機能、未検証の外部依存を整理し、振り返りを記録する。

## 実装後の振り返り

### 実施日

2026-09-11

### 確認結果

- Lobby、Gateway、token、SQLite状態機械のPythonテストは12件すべて成功した。
- Godotの試合シミュレーション、Dedicated RPC契約、ロビーUIテスト、およびエディタのヘッドレス起動は終了コード0で完了した。生成済みWindowsクライアントもヘッドレスで起動できた。
- PowerShellスクリプト13本は構文解析を通過した。
- ただし、Dedicated Serverが試合終了を検出すると`close_terminal()`を呼ぶため、既存の結果画面の再戦・ロビー復帰要求が常に拒否される回帰を静的に確認した。
- 新規ユーザー／既存設定を持たない開発者は、ローカルLobby URLと招待tokenの初期値が空で、HTTP許可設定もないため、従来の`start-local.ps1`によるオンライン接続を開始できない。
- Dedicated Serverの異常終了後に`CONNECTED`予約を回収・再同期する仕組みがなく、永続SQLiteに幽霊ルームが残るリスクを確認した。
- Funnel検査は`127.0.0.1:8080`という文字列の存在だけを確認しており、ほかの公開targetの混在を検出できない。外部公開前のsecurity gateとして不十分である。

### 未検証範囲

- `user://settings.cfg`への保存・再起動後の読込は、この実行環境の書込み制限で確認できなかった。
- Tailscale Funnel、playit.gg、別回線を通す実通信は実アカウントと外部状態変更を必要とするため実施していない。

### 次回への改善提案

- Lobbyの公開状態を閉じる処理と、接続済み2人による再戦状態を分離し、Dedicated Serverを含む結果画面統合テストを追加する。
- ローカル開発用のURL・招待tokenを安全にクライアントへ渡す明示的な導線を追加し、`start-local.ps1`から実際に対戦できる回帰テストを作る。
- Dedicated Server再起動時の予約回収／reconciliationと、Funnel JSONを厳密に解析する事前検査を実装する。
