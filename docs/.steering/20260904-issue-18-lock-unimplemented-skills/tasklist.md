# タスクリスト

## フェーズ1: 仕様・調査

- [x] issue-18 の依頼原文を `request.md` に記録する
- [x] 要求事項と実装方針を `requirements.md` / `design.md` に整理する
- [x] 既存の候補名、アイコン、クリック接続、HUD初期アイコンを確認する

## フェーズ2: 実装

- [x] キャラクター別の候補名・実装状態・アイコン設定を修正する
- [x] 未実装候補のロック表示、暗色化、選択防止を実装する
- [x] 未実装候補がロードアウトへ混入しないよう状態更新を防御する
- [x] 打鍵士通常攻撃HUDをプレースホルダーアイコンへ変更する

## フェーズ3: 検証

- [x] Godotヘッドレスでシーン・スクリプトエラーがないことを確認する
- [x] キャラクター画面の候補表示と選択防止を確認する
- [x] 対戦HUDの通常攻撃アイコンと既存スキル表示を確認する

## 実装完了後の振り返り

### 実装完了日

2026-09-04

### 計画と実績の差分

候補の固定レイアウトは変更せず、既存ボタンへロック画像・暗色スタイル・disabled状態を適用した。未実装候補の確認モーダル経由の反映も防御し、候補名は各キャラ仕様書の正式名称へ修正した。ロック画像はGodotのインポートを実行して利用可能にした。

### 検証結果

`godot --headless --path . --quit-after 3` が起動完了し、プロジェクトスクリプトのパースエラーは発生しなかった。既存の `user://logs` 保存権限警告と証明書ストア警告は環境由来で、起動は継続した。

### 学んだこと・次回への改善

未実装状態は表示だけでなく、UI入力・確認処理・ロードアウト更新の各経路で防御すると状態の混入を防ぎやすい。次回はGodotエディター上で各キャラクターのロック画像の重なり方を目視確認する。
