# タスクリスト

- [x] 既存の通常攻撃判定・円弧描画・キャラクター識別子を確認する
- [x] エフェクト素材を64×64pxの透明背景ピクセルスプライトへ軌道修正する
- [x] 1枚絵ではなく、各職4コマの細い軌跡のスプライトシートへ軌道修正する
- [x] 3職×4コマの64×64px通常攻撃ピクセルスプライトを生成・正規化する
- [x] 4コマを横並びにした256×64pxスプライトシートを`assets/effects/`へ配置する
- [x] 職業別のスプライト描画とアニメーションを実装し、円弧描画を置き換える
- [x] Godotを起動してスクリプトエラーと通常攻撃表示を確認する
- [x] issue-9をアーカイブし、ステアリングの振り返りを記録する

## 実装完了後の振り返り

実装完了日: 2026-09-01

- 各職の通常攻撃を、64×64pxのRGBAピクセルスプライト4コマを横に並べた256×64pxシートへ置き換えた。
- 打鍵士・詠唱者は細い斬撃軌跡、算術士はフェードインする積分記号として差別化し、各コマを攻撃時間に合わせて切り替えるようにした。
- `try_attack()`、`SLASH_RANGE`、`SLASH_ARC_HALF_ANGLE`は変更していないため、命中判定・ダメージ・ネットワーク同期形式は既存仕様を維持する。
- Godot 4.7.2のヘッドレス起動で新規PNGのインポートとスクリプト読込を確認し、プロジェクトのスクリプトエラーは発生しなかった。環境由来の証明書ストア・ユーザーログ保存警告は残るが、起動を妨げなかった。
