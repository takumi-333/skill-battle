# タスクリスト

- [x] 既存の隕石定数、落下地点選択、描画時間を確認する。
- [x] ローカルとサーバーの予約間隔、着弾時間、落下地点範囲を新仕様へ変更する。
- [x] 予告影と着弾波紋の補間を新仕様へ変更する。
- [x] 仕様書と回帰テストを更新する（仕様書はすでに依頼内容と一致していたため、回帰テストのみ更新）。
- [x] Godotテスト、ヘッドレス起動、差分検査を実行し、結果を記録する。

## 検証結果

- 2026-09-13: `godot --headless --path . --script res://tests/network/match_simulation_test.gd` が成功。20地点すべてのマップ内・対象から80〜640px、1.25秒間隔、3.5秒後の着弾、影半径内外のダメージを確認した。
- 2026-09-13: `godot --headless --path . --script res://tests/network/lobby_ready_ui_test.gd` が終了コード0で成功。既存の `user://` 非書き込み環境に起因するロビー設定アサート表示、ログ出力・証明書ストアの環境警告は発生したが、最終出力は `online lobby ready UI tests passed` だった。
- 2026-09-13: `godot --headless --path . --quit-after 3` が終了コード0で成功。`user://logs` とWindows証明書ストアの環境警告のみ確認した。
- 2026-09-13: `git diff --check` が成功し、空白エラーはなかった。

## 実装後の振り返り

- 実装完了日: 2026-09-13
- 計画との差分: 既存の仕様書は依頼内容へすでに更新されていたため、コードと回帰テストの同期に集中した。
- 学び: マップ端のクランプは距離要件を壊し得るため、候補を採用する前に境界と距離を同時に検証する必要がある。
