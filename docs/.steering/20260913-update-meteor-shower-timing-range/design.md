# 設計

クライアントの `match_prototype.gd` とサーバー権威の `match_simulation.gd` にある隕石定数を同じ値へ変更する。落下地点はクランプによって距離範囲が崩れないよう、ランダム候補を採用する前にマップ内・対象距離80〜640pxを検証する。候補が不足する場合も、対象からアリーナ中心へ向かう安全な方向で最小距離を満たす地点を返す。

描画側では影の開始半径と波紋の開始・終了半径を定数化し、更新した継続時間に合わせて補間する。ネットワークシミュレーションテストは予約間隔、3.5秒の着弾時点、マップ内・距離範囲を検証する。
