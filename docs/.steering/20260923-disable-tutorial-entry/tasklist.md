# タスクリスト

## フェーズ1: チュートリアル入口の停止

- [x] ホーム画面ボタンを「チュートリアル（準備中）」にし、シーン上でdisabledにする。
- [x] 実行時初期化でボタンを無効化し、チュートリアル遷移要求を抑止する。

## フェーズ2: 仕様と確認

- [x] ホーム画面仕様に準備中の状態を反映する。
- [x] Godotをヘッドレス起動して読み込みを確認する（`ready complete`を確認。ログ書き込み不可と証明書ストアの警告あり）。
- [x] `git diff --check` を実行する（今回の変更箇所は問題なし。既存変更の `docs/specs/画面一覧仕様書/CPU対戦ロビー.md` 27行目に末尾空行を検出）。

## 実装後の振り返り

### 実装完了日

2026-09-23

### 計画と実績の差分

**計画と異なった点**:
- 全体の `git diff --check` は今回の作業と無関係な既存変更の末尾空行を検出したため、該当ファイルには手を加えず記録した。

**新たに必要になったタスク**:
- なし。

### 学んだこと

- 無効状態はシーン保存と実行時初期化の両方で設定し、画面遷移の入口にもガードを置くと確実。

### 次回への改善提案
- なし。
