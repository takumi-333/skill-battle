# タスクリスト

- [x] 隕石画像の可視下端座標と現行アンカー計算を確認する。
- [x] 可視下端中央を基準に隕石描画矩形を計算するよう修正する。
- [x] 可視下端と着弾点の一致を確認する回帰テストを更新する。
- [x] Godotテスト、ヘッドレス起動、差分検査を実行し、結果を記録する。

## 検証結果

- 2026-09-13: `godot --headless --path . --script res://tests/network/match_simulation_test.gd` が成功した。
- 2026-09-13: `godot --headless --path . --script res://tests/network/lobby_ready_ui_test.gd` が終了コード0で成功し、可視下端（85/96の描画高）と着弾点の一致を確認した。既存の `user://` 非書き込み環境に起因するロビー設定アサート表示、ログ出力・証明書ストア・ObjectDBの環境警告は発生したが、最終出力は `online lobby ready UI tests passed` だった。
- 2026-09-13: `godot --headless --path . --quit-after 3` が終了コード0で成功した。`user://logs` とWindows証明書ストアの環境警告のみ確認した。
- 2026-09-13: `git diff --check` が成功し、空白エラーはなかった。

## 実装後の振り返り

- 実装完了日: 2026-09-13
- 計画との差分: 当初は画像矩形下端を着弾点にしていたが、画像の透明余白を除いた可視下端へ基準を変更した。
- 学び: スプライトを地上アンカーに合わせるときは、テクスチャ矩形ではなく不透明ピクセルの範囲を仕様として扱う必要がある。
