# 設計

`falling_meteor.png` は64×96pxで、アルファ値がある最下端は y=85px である。描画サイズは縦横比を保って拡大されるため、可視下端の比率 `85 / 96` を定数化する。

`meteor_texture_rect()` は着弾点から、描画幅の半分と可視下端比率ぶんの描画高を差し引いて描画矩形を返す。これにより、可視下端中央が常に着弾点へ来る。落下中はこの着弾点だけを上方へ補間するため、同じアンカー規則を保持できる。
