# 要求事項

## 目的

流月雨の隕石を、画像矩形の下端ではなく、実際に描かれている隕石の最下端中央へ合わせる。

## 機能要件

- `falling_meteor.png` の透明余白を着弾アンカーに含めない。
- 描画済みピクセルの最下端（元画像の y=85px）の中央が、影と波紋の中心である着弾点に一致する。
- 落下中と着弾直後で同じ可視下端アンカーを使う。
- 既存の影中心・着弾時のダメージ範囲は変更しない。

## 検証要件

- 描画矩形における可視下端の座標が着弾点と等しいことをテストする。
- Godotの関連テスト、ヘッドレス起動、差分の空白検査を実行する。
