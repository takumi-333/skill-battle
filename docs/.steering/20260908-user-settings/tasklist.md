# タスクリスト

## フェーズ1: 設定 UI と永続化

- [x] ホーム画面に人物アイコンの設定ボタンとユーザー設定モーダルを追加する。
- [x] モーダルの開閉・入力検証・`user://settings.cfg` への保存と復元を実装する。

## フェーズ2: 表示とネットワーク同期

- [x] 待機画面・リザルト画面がユーザー名を表示するようにする。
- [x] 専用サーバーのロードアウト、プロトコル検証、スナップショットにユーザー名を接続する。
- [x] 再戦時もユーザー名を保持する。

## フェーズ3: 検証

- [x] 通信・シミュレーションテストをユーザー名ケースで拡充する。
- [x] Godot ヘッドレステストを実行する。
- [x] Godot を起動してエラーログを確認する。

## 実装後の振り返り

実施日: 2026-09-08

- `match_simulation_test.gd` はユーザー名の検証・ロードアウト反映を含めて成功した。
- `godot --headless --quit-after 3 --path .` でプロジェクトを起動し、今回の UI・スクリプトに起因するパースエラーはなかった。
- `rpc_contract_test.gd` は既存のクライアント／サーバー RPC 契約比較で assertion が発生した。テストと同じ抽出ロジックでキー数・各 RPC 指定を確認すると 19 件すべて一致しており、今回の名前同期変更は RPC 定義を変更していない。

## フェーズ4: 表示役割の調整

- [x] ロビーの自分枠をキャラクター名、相手枠を同期済みユーザー名だけの表示に分ける。
- [x] リザルトをプレイヤー名専用表示にし、キャラクター名フォールバックをなくす。
- [x] ヘッドレステストとプロジェクト起動を実行する。

### 追記（2026-09-08）

- `match_simulation_test.gd` が成功し、名前同期済みフラグの検証も通過した。
- ヘッドレス起動で今回の変更に起因するパースエラーは検出されなかった。
