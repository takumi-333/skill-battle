# 設計

- ノイズ状態のプレイヤーへ、状態を付与した算術士のプレイヤーIDを保持する。
- 通常攻撃の直接命中判定が失敗したときだけ、状態の有効時間中であることと発動者が算術士であることを確認する。
- 条件を満たすと、空振りした相手位置から発動者へ向かう既存の妨害ポイント到着演出を生成し、到着時に0.5を加算する。
- サーバー権威シミュレーションのテストで、命中では加算されず、空振りでは0.5だけ加算され、状態終了後には加算されないことを確認する。
