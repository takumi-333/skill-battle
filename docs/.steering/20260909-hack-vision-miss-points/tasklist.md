# タスクリスト

- [x] 視覚妨害の発動者をノイズ状態へ保持し、空振り時の0.5ポイント付与をローカル・サーバー権威ロジックへ実装する。
- [x] スキル仕様と自動テストを更新し、Godotテスト・ヘッドレス起動で検証する。

## 実装後の振り返り

- 実装完了日: 2026-09-09
- 実績: ノイズ状態に発動者IDを保持し、被妨害者の通常攻撃が直接命中しなかったときだけ、発動した算術士へ`+0.5`の妨害ポイント到着演出を生成するようにした。
- 判定: デコイだけを壊した通常攻撃は空振りとして加算し、プレイヤーへの直接命中およびノイズ状態の終了後は加算しない。
- 検証: サーバー権威テストで空振り・命中・状態終了後の3ケースを確認した。ヘッドレス起動に本変更由来のエラーはない。既存の通常攻撃ダメージ期待値に関する2件の失敗は残っている。
