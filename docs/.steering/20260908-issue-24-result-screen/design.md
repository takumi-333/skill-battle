# 設計

- `scenes/ui/result_screen.tscn` に、素材表示用の `TextureRect` と数値表示用の `Label` を定義する。ボタンの素材はクリック可能な `Button` の背面に置く。
- `MatchPrototype` はシーン内ノードを取得し、試合終了時に勝者／敗者、残HP、平均スキルスコア、経過時間を各ラベルへ設定する。
- 従来の要約ラベルは互換性のため維持し、オンライン再戦待機文を表示する。画面の主な結果表示は個別ラベルで行う。
- 画面素材の背面には暗いグラデーション風の配色と枠線を置き、敗者側は半透明の暗色パネルで抑える。
