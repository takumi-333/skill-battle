# 設計

スキル1では、未出現状態の魔法陣の位置は `Vector2.ZERO`、出現時の位置は相手位置である。補間時に `spawned` が false から true へ変わったエンティティの `position` を補間しないことで、出現フレームから確定位置を使う。

スキル2では、汎用弾の初回更新処理が未発射弾を相手方向へ再設定する。詠唱士の大技弾だけは生成時点で発射済みとして扱い、16方向の初期ベクトルと大技用速度を保持する。打鍵士の弾は従来どおり未発射として扱う。

サーバー権威シミュレーションに、魔法陣出現時の補間位置と詠唱士大技弾の放射方向を検証するテストを追加する。
