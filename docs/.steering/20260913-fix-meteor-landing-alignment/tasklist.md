# タスクリスト

- [x] 流月雨の隕石描画アンカーと着弾判定の現在の挙動を確認する。
- [x] 隕石下部中央を着弾点に揃え、影の最終半径とダメージ範囲を統一する。
- [x] 着弾範囲内・範囲外を検証する回帰テストを追加する。
- [x] Godotテスト、ヘッドレス起動、差分検査を実行し、結果を記録する。

## 検証結果

- 2026-09-13: `godot --headless --path . --script res://tests/network/match_simulation_test.gd` が成功。影の半径48pxちょうどの位置で命中し、1px外では命中しない回帰テストを確認した。
- 2026-09-13: `godot --headless --path . --script res://tests/network/lobby_ready_ui_test.gd` が終了コード0で成功。隕石描画矩形の下辺中央と着弾点の一致を確認した。既存の `user://` 非書き込み環境に起因するロビー設定アサート表示、ログ出力・証明書ストア・ObjectDBの環境警告は発生したが、テストの最終出力は `online lobby ready UI tests passed` だった。
- 2026-09-13: `godot --headless --path . --quit-after 3` が終了コード0で成功。スクリプトエラーは発生しなかった。`user://logs` とWindows証明書ストアの環境警告のみ確認した。
- 2026-09-13: `git diff --check` が成功。空白エラーはない。

## 実装後の振り返り

- 実装完了日: 2026-09-13
- 判定範囲を隕石画像から独立させ、地上の着弾アンカーを影・波紋・ダメージで共有した。
- 隕石テクスチャを中心基準で描画していたことがずれの原因だった。下辺中央をアンカーにするヘルパーへ集約し、着弾時と落下中で同じ規則を使うようにした。
