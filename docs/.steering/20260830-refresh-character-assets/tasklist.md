# タスクリスト

## 実装

- [x] 更新済み仕様とアセット制作ガイドから、3キャラクターのデザイン契約を確定する。
- [x] 打鍵者の立ち絵と8方向スプライトシートを生成・正規化し、既存素材を置き換える。
- [x] 算術士の立ち絵と8方向スプライトシートを生成・正規化し、既存素材を置き換える。
- [x] 詠唱者の立ち絵と8方向スプライトシートを生成・正規化し、既存素材を置き換える。
- [x] 詠唱者スプライトの縦横比とフレーム位置を再正規化し、横長表示とちらつきを修正する。
- [x] 詠唱者スプライトの各セルから孤立した混入ピクセルを除去する。
- [x] 対戦画面の表示用ID、方向列、停止・歩行行、描画色の実装を点検し、必要な修正を行う。

## 検証

- [x] 各スプライトが512×320pxのRGBA PNGで、8列×5行の40フレームを備えることを検査する。
- [x] 立ち絵とスプライトのデザイン契約への一致、およびセル外混入・切れ端・欠けがないことを目視確認する。
- [ ] Godotを起動して対戦画面の表示とログを確認し、検証結果を記録する。

## 実装後の振り返り

- 実施日: 2026-08-30
- 実施内容: `typist`、`arithmetician`、`chanter` の立ち絵と8方向スプライトを更新し、スプライトを64×64px×40セルの512×320px RGBA PNGへ正規化した。詠唱者は縦長の生成セルを縦横別に縮小していたため、縦横比を維持して足元基準で再配置し直した。さらに、各セルで最大の連結成分だけを残し、孤立した混入ピクセルを除去した `chanter_pixel_8dir_clean.png` を作成し、対戦画面の参照を切り替えた。
- 検査結果: 各シートの40セルすべてで非透明画素を確認した。詠唱者のクリーン版は、目視で確認できた杖状・粒状の孤立成分を含まない。表示コードは表示用ID、45度方向、停止行・8fps歩行行、`Color.WHITE` 描画を満たす。
- 未実施: この環境ではGodot実行ファイルがPATH上になく、起動・ログ確認を実行できなかった。
