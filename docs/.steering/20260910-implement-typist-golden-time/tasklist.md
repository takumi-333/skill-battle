# タスクリスト

## フェーズ1: 選択・課題定義

- [x] 打鍵士のスキル1/2/3に黄金時間候補と作成済みアイコンを接続する。
- [x] レベル別の課題候補、制限時間、クールダウン、持続時間を実装する。

## フェーズ2: ゾーン効果

- [x] ローカル対戦にゾーン状態、通常攻撃不可、入力時のキーキャップ・スコア補正・III波動を実装する。
- [x] 専用サーバーシミュレーションに同等のゾーン効果を実装する。
- [x] ゾーン中の視覚フィードバックと HUD 表示を追加する。

## フェーズ3: 検証

- [x] Godot ヘッドレス起動と関連する実行確認を行う。
- [x] 実装結果と検証結果を振り返りへ記録する。

## 実装後の振り返り

### 実装日

2026-09-10

### 実装内容と要求の差分

- スキル候補、課題、ゾーンの単一状態、スコア補正、鍵片、III の波動、通常攻撃不可、作成済みアイコン、視覚表示を実装した。
- ローカル対戦と `MatchSimulation` の双方で同じレベル別のクールダウン・持続時間・入力効果を扱う。

### 確認したこと

- `godot --headless --path . --quit-after 12` でプロジェクトを起動し、GDScript の解析エラーがないことを確認した。
- 一時的な `MatchSimulation` スモークテストで、I の発動と通常攻撃不可、III への更新、正解入力時の鍵片と波動の生成を確認した。

### 次回への改善点・注意点

- 専用サーバーを実際に起動するには `SKILL_BATTLE_TOKEN_SECRET` が必要なため、今回の検証はシミュレーション単体に留めた。
