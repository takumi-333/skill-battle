# SNES Faithful Converter - 設計方針

## 1. プロジェクト概要

本プロジェクトは、MP3 / WAV / FLAC などの楽曲を入力として、**原曲のメロディ・コード進行・BPM・リズム・構成・ベースラインを極力損なわずに、16bit時代のスーパーファミコン（SNES / SFC）風の音像へ変換するツール**を開発するものである。

目標は「大胆なチップチューンアレンジ」ではなく、次の感覚に近い出力を得ること。

> 原曲そのものを、スーパーファミコン風の音源で演奏し直したように聞こえること。

本プロジェクトでは、音声解析・音符抽出・レンダリングなどは可能な限り**決定論的な機械処理**で行い、AIは必要最小限の「編曲判断」にだけ利用する。

---

# 2. 最重要設計原則

## 2.1 原曲再現を最優先する

以下は原則として変更禁止とする。

- 主旋律
- 音程
- コード進行
- BPM
- リズム
- 小節位置
- 曲構成
- ベースライン
- セクション境界
- フレーズ開始位置

以下はSNES化のために変更可能とする。

- 楽器音色
- 同時発音数
- コード構成音の間引き
- パート統合
- オクターブ調整
- ベロシティ調整
- パンニング
- ADSR
- Echo / FIR
- サンプル選択
- BRR化
- Voice共有

思想としては以下。

```text
作曲内容は保存する
音源・オーケストレーションだけSNESへ寄せる
```

---

## 2.2 AIに音楽そのものを生成させない

AIに新しいメロディやコードを書かせてはいけない。

AIの責務は以下に限定する。

- どのパートをVoice 1〜8へ割り当てるか
- どのSNES音色を選ぶか
- 同時発音数が多い場合に、どのパートを統合するか
- どのコード構成音を削減可能とするか
- どのパートを優先して保持するか
- セクションごとのVoice再利用方針
- 原曲を壊さない範囲でのオクターブ調整

AIが直接 NoteEvent / MIDI ノート列を書き換える実装は禁止する。

---

## 2.3 AI依存をCoreへ持ち込まない

Converter Core は ChatGPT / Codex / OpenAI API / MCP / Vercel に依存してはいけない。

Coreは以下のような通常プログラムとして完結させる。

```text
analyze
validate
arrange
render
```

AIは `analysis.json -> song.yaml` の変換を補助する外部レイヤーとして扱う。

---

# 3. システム全体構成

```text
┌──────────────────────┐
│ Original Song         │
│ MP3 / WAV / FLAC      │
└──────────┬───────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ ANALYZER                             │
│ 機械処理                             │
│                                     │
│ ・Decode / Normalize                │
│ ・Stem separation                   │
│ ・BPM / Key                         │
│ ・Pitch / MIDI extraction           │
│ ・Chord analysis                    │
│ ・Drum analysis                     │
│ ・Section analysis                  │
└──────────┬──────────────────────────┘
           │
           ▼
      analysis.json
           │
           │
           ├─────────────┐
           │             │
           ▼             ▼
      ai_prompt.md    人間確認
           │
           ▼
┌─────────────────────────────────────┐
│ AI / Codex / ChatGPT                │
│                                     │
│ 編曲判断のみ                         │
│ analysis.json + prompt              │
│           ↓                         │
│ song.yaml                           │
└──────────┬──────────────────────────┘
           │
           ▼
┌─────────────────────────────────────┐
│ SNES CONVERTER CORE                 │
│                                     │
│ ・Schema validation                 │
│ ・Rule validation                   │
│ ・Voice allocation                  │
│ ・Harmony reduction                 │
│ ・Instrument mapping                │
│ ・SNES style rendering              │
│ ・BRR / ADSR / Echo                 │
└──────────┬──────────────────────────┘
           │
           ▼
      output.wav
      output.spc (将来)
```

---

# 4. 初期アーキテクチャ

## 4.1 MVP

MVPではWebアプリを作らない。

以下の構成とする。

```text
Python Project
+
CLI
+
Manual AI Handoff
```

操作イメージ。

```bash
snes analyze input/song.mp3
```

出力。

```text
work/analysis.json
work/ai_prompt.md
```

ユーザーが `analysis.json` と `ai_prompt.md` を ChatGPT / Codex 等へ手動で渡し、AIが `song.yaml` を生成する。

生成結果を以下へ配置。

```text
work/song.yaml
```

その後。

```bash
snes validate work/song.yaml
snes render work/song.yaml
```

出力。

```text
output/song_snes.wav
```

---

# 5. 将来のCodex連携

MVP完成後、Codexがリポジトリ上でCLIを操作できる状態にする。

ユーザー操作は最終的に以下を目標とする。

```text
input/song.mp3 を原曲再現優先でSNES風へ変換して
```

Codexが内部で以下を実施する。

```text
1. analyze を実行
2. analysis.json を読む
3. instrument catalog を読む
4. song.yaml を作成
5. validate を実行
6. Validation Error があれば修正
7. render を実行
8. output.wav を生成
```

重要:

Codexは音響解析エンジンではない。

Codexは以下の判断部分のみ担当する。

```text
analysis.json
+
SNES Rules
+
Instrument Catalog
↓
song.yaml
```

---

# 6. MCPについて

MCPはMVPでは実装しない。

CLIが安定した後に必要であれば追加する。

将来想定するMCP Tools。

```text
analyze_song
get_song_analysis
get_instruments
validate_config
render_song
```

将来構成。

```text
        Codex / ChatGPT / Agent
                 │
                MCP
                 │
      ┌──────────┼──────────┐
      ▼          ▼          ▼
   analyze    validate     render
      │          │          │
      └──────────┴──────────┘
                 │
         SNES Converter Core
```

CoreはMCPに依存しないこと。

---

# 7. Web / Vercel 方針

MVPでは不要。

Web UIが必要になった場合も、Vercelは原則としてUI層のみとする。

```text
Vercel / Next.js
      │
      ▼
Processing Backend
Python / Container
      │
      ▼
SNES Converter Core
```

音源分離、PyTorch推論、ffmpeg、DSP等の重い処理をVercel Functionへ強く依存させない。

---

# 8. 推奨ディレクトリ構成

```text
snes-faithful-converter/

├─ README.md
├─ pyproject.toml
├─ .gitignore
│
├─ input/
│   └─ .gitkeep
│
├─ output/
│   └─ .gitkeep
│
├─ work/
│   ├─ .gitkeep
│   ├─ analysis.json
│   ├─ ai_prompt.md
│   └─ song.yaml
│
├─ src/
│   └─ snes_converter/
│       ├─ __init__.py
│       ├─ cli.py
│       ├─ pipeline.py
│       │
│       ├─ analyzer/
│       │   ├─ __init__.py
│       │   ├─ audio_loader.py
│       │   ├─ separator.py
│       │   ├─ tempo.py
│       │   ├─ key.py
│       │   ├─ pitch.py
│       │   ├─ chord.py
│       │   ├─ drums.py
│       │   └─ structure.py
│       │
│       ├─ music/
│       │   ├─ __init__.py
│       │   ├─ song_ir.py
│       │   ├─ note.py
│       │   ├─ chord.py
│       │   └─ track.py
│       │
│       ├─ ai/
│       │   ├─ __init__.py
│       │   ├─ packet.py
│       │   ├─ prompt.py
│       │   └─ schema.py
│       │
│       ├─ arranger/
│       │   ├─ __init__.py
│       │   ├─ arranger.py
│       │   ├─ voice_allocator.py
│       │   ├─ harmony_reducer.py
│       │   ├─ octave_optimizer.py
│       │   └─ drum_mapper.py
│       │
│       ├─ snes/
│       │   ├─ __init__.py
│       │   ├─ instruments.py
│       │   ├─ renderer.py
│       │   ├─ adsr.py
│       │   ├─ echo.py
│       │   ├─ fir.py
│       │   └─ brr.py
│       │
│       └─ validation/
│           ├─ __init__.py
│           ├─ schema.py
│           └─ rules.py
│
├─ instruments/
│   ├─ catalog.yaml
│   └─ samples/
│
├─ profiles/
│   ├─ faithful.yaml
│   ├─ balanced.yaml
│   └─ hardware.yaml
│
├─ schemas/
│   ├─ analysis.schema.json
│   └─ song.schema.json
│
└─ tests/
    ├─ test_analysis.py
    ├─ test_validation.py
    ├─ test_voice_allocator.py
    └─ test_renderer.py
```

---

# 9. 内部データモデル

最重要なのは、Audio解析結果とSNES設定を直接結合しないこと。

以下の中間表現を利用する。

```text
Audio
↓
SongIR
↓
Arrangement Config
↓
SNES Rendering
```

---

## 9.1 SongIR

例。

```python
class SongIR:
    bpm: float
    key: str | None
    time_signature: tuple[int, int] | None

    sections: list[Section]
    tracks: list[Track]
    metadata: dict
```

Track例。

```python
class Track:
    id: str
    role: str
    notes: list[NoteEvent]
    confidence: float
    source_stem: str | None
```

NoteEvent例。

```python
class NoteEvent:
    start: float
    duration: float
    pitch: int
    velocity: int
    confidence: float
```

---

# 10. analysis.json

AIが読むための構造化データ。

巨大な生MIDIをそのままLLMへ投げない。

必要に応じて要約情報と詳細情報を分離する。

例。

```json
{
  "song": {
    "bpm": 124,
    "key": "C major",
    "duration": 213.4
  },
  "sections": [
    {
      "name": "intro",
      "start": 0.0,
      "end": 12.3
    },
    {
      "name": "verse",
      "start": 12.3,
      "end": 45.8
    },
    {
      "name": "chorus",
      "start": 45.8,
      "end": 78.4
    }
  ],
  "tracks": [
    {
      "id": "vocal",
      "role": "melody",
      "importance": 1.0,
      "pitch_range": ["E3", "C5"],
      "polyphony": 1
    },
    {
      "id": "bass",
      "role": "bass",
      "importance": 0.95,
      "pitch_range": ["A1", "E3"],
      "polyphony": 1
    },
    {
      "id": "piano",
      "role": "harmony",
      "importance": 0.75,
      "polyphony": 6
    }
  ]
}
```

---

# 11. AI Prompt設計

`ai_prompt.md` を機械生成する。

Promptの最重要ルール。

```text
あなたはSNES向けの編曲プランナーである。

目的は「新しく編曲すること」ではない。

原曲の音楽的内容を可能な限り維持し、
SNES風の音源・Voice構成へ写像することである。

次を変更してはいけない。

- melody
- chord progression
- tempo
- rhythm
- bassline
- song structure

新しい音符を追加してはいけない。

既存のNoteEventを書き換えてはいけない。

あなたが判断可能なのは以下のみ。

- voice allocation
- instrument selection
- part merge
- harmony reduction
- octave shift
- priority
- section-specific voice reuse

結果は指定されたYAML Schemaに完全準拠させること。
```

---

# 12. song.yaml

AIが生成する設定。

例。

```yaml
version: 1

mode: faithful

preservation:
  melody: strict
  bass: strict
  rhythm: strict
  harmony: high
  structure: strict

snes:
  max_voices: 8

voice_allocation:
  - voice: 1
    source: vocal
    role: melody
    instrument: lead_strings
    priority: 100

  - voice: 2
    source: bass
    role: bass
    instrument: electric_bass
    priority: 95

  - voice: 3
    source: piano
    role: harmony
    instrument: piano
    priority: 75

  - voice: 4
    source: guitar
    role: harmony
    instrument: guitar
    priority: 70

  - voice: 5
    source: strings
    role: support
    instrument: strings
    priority: 60

  - voice: 6
    source: drums.kick
    role: kick
    instrument: kick_01
    priority: 90

  - voice: 7
    source: drums.snare
    role: snare
    instrument: snare_01
    priority: 85

  - voice: 8
    source:
      - drums.hihat
      - drums.crash
    role: cymbal
    instrument: cymbal_01
    priority: 50

reduction:
  strategy: minimal
  preserve_melody: true
  preserve_bass: true
  preserve_chord_root: true
  allow_harmony_reduction: true
  allow_part_merging: true
  allow_note_generation: false

render:
  echo_preset: medium
  stereo: true
```

---

# 13. Validation

AI出力を信用しない。

必ずSchema ValidationとRule Validationを通す。

## Schema Validation

Pydantic または JSON Schema を使用する。

例。

- 必須フィールド不足
- 不正型
- 存在しないenum
- Voice番号不正

---

## Rule Validation

以下を検査する。

- max_voicesを超えていないか
- Instrument Catalogに存在しない音色を参照していないか
- melodyが削除されていないか
- basslineが削除されていないか
- `allow_note_generation == false` であること
- MIDIノート自体をAI設定で書き換えていないこと
- 許可範囲外のoctave shiftがないこと
- 同時発音上限違反
- Section overrideの矛盾

Validation Errorは人間/Agentが理解可能な形式で返す。

例。

```text
ERROR E102:
voice_allocation uses instrument "super_lead_x",
but this instrument does not exist in instruments/catalog.yaml.

Allowed candidates:
- lead_strings
- square_lead
- flute_01
```

---

# 14. Voice Allocation

基本はSNESの8 Voiceを意識する。

ただし本プロジェクトでは「SNES実機完全再現」より原曲再現を優先する。

優先度のデフォルト例。

```text
Melody            100
Bass               95
Kick               90
Snare              85
Harmony Root       80
Harmony Third      75
Counter Melody     65
HiHat              50
Decoration         30
```

同時発音数超過時。

```text
1. Melodyは保持
2. Bassは保持
3. Kick/Snareを優先
4. Harmonyを縮約
5. 同一役割Partを統合
6. Decorationを最後に削る
```

---

# 15. Harmony Reduction

原曲のコード進行は変えない。

構成音のみ必要最小限で縮約可能。

例。

```text
C E G B
↓
C E B
```

BassがCを担当している場合。

```text
Bass: C
Harmony: E + B
```

のような重複削減を許可する。

禁止事項。

```text
Cmaj7
↓
Am
```

のように和声機能そのものを変更してはいけない。

---

# 16. Instrument Catalog

AIは任意の楽器名を作ってはいけない。

すべて `instruments/catalog.yaml` から選択する。

例。

```yaml
instruments:

  lead_strings:
    family: strings
    root_note: C4
    sample: samples/lead_strings.wav
    role:
      - melody
      - harmony

  electric_bass:
    family: bass
    root_note: C2
    sample: samples/electric_bass.wav
    role:
      - bass

  kick_01:
    family: drums
    sample: samples/kick_01.wav
    role:
      - kick
```

---

# 17. SNES Rendering

MVPではまず「SNES風WAV」を成立させる。

実機完全互換は後段とする。

MVP候補。

```text
MIDI / NoteEvent
↓
SNES Sample Library
↓
Pitch Shift
↓
ADSR
↓
Pan
↓
SNES-style Echo
↓
WAV
```

Phase 2以降で追加。

```text
BRR encoding
Gaussian interpolation
SNES ADSR compatibility
8-tap FIR
SPC output
APU memory constraints
```

---

# 18. 変換モード

将来的に3モードを持たせる。

## faithful

デフォルト。

```text
原曲再現最優先
```

多少SNES実機制約を緩めても、音楽的情報を残す。

---

## balanced

```text
原曲再現
+
SNES実機らしさ
```

の中間。

---

## hardware

```text
SNES実機制約最優先
```

8voice、メモリ、サンプル、DSP制約を厳密に適用する。

MVPは `faithful` のみ実装してよい。

---

# 19. 推奨技術

初期実装。

```text
Python 3.12+
Typer              CLI
Pydantic           Schema / Validation
numpy              DSP
scipy              Signal Processing
soundfile          Audio I/O
ffmpeg             Decode / Encode
librosa            Tempo / Chroma等
pretty_midi        MIDI
PyTorch            MLモデル実行
```

Stem分離・Pitch解析・Chord解析は交換可能なAdapterとして設計する。

特定ライブラリへCoreを密結合させない。

例。

```python
class StemSeparator(Protocol):
    def separate(self, audio_path: Path) -> StemResult:
        ...
```

---

# 20. CLI仕様

最低限以下を実装する。

## Analyze

```bash
snes analyze input/song.mp3
```

出力。

```text
work/analysis.json
work/ai_prompt.md
```

---

## Validate

```bash
snes validate work/song.yaml
```

成功。

```text
Validation OK
```

失敗。

```text
Validation Failed
...
```

---

## Render

```bash
snes render work/song.yaml
```

出力。

```text
output/song_snes.wav
```

---

## 将来: Convert

Codex / Agent向け。

```bash
snes convert input/song.mp3
```

AI連携可能時はAnalyzeからRenderまで一括。

ただしMVPでは実装不要。

---

# 21. ログ

機械処理の判断を確認できるようにする。

例。

```text
[ANALYZE] BPM detected: 124.1
[ANALYZE] Key detected: C major
[ANALYZE] Vocal stem detected
[ANALYZE] Bass stem detected
[AI] Packet generated: work/ai_prompt.md
[VALIDATE] voices: 8/8
[VALIDATE] melody preservation: OK
[VALIDATE] bass preservation: OK
[RENDER] rendering...
[RENDER] output/song_snes.wav
```

AIの判断と機械処理の判断をログ上で区別する。

---

# 22. エラー処理

解析失敗時に黙って補完しない。

Confidenceが低い場合は明示する。

例。

```json
{
  "key": {
    "value": "A minor",
    "confidence": 0.42
  }
}
```

Confidence閾値未満の場合。

```text
WARNING:
Key detection confidence is low.
AI must not assume this value is correct.
```

ハルシネーション防止のため、不明値は `null` とする。

---

# 23. AI Agentを使う場合のTools

将来的にAgentを組む場合、まずCLI Wrapperのみ提供する。

概念的には以下。

```text
analyze_song(path)
get_analysis()
get_instruments()
validate_config(path)
render_song(path)
```

AIが直接Python内部関数へ密結合しない。

CLI / Tool境界を保つ。

---

# 24. テスト戦略

最低限以下をテストする。

## Unit Tests

```text
SongIR parsing
Schema validation
Voice allocation
Harmony reduction
Instrument lookup
Octave constraints
```

## Integration Tests

短いfixture音源を用意。

```text
test_melody.wav
test_chord.wav
test_bass.wav
```

以下を検証。

```text
audio
↓
analysis
↓
manual YAML
↓
render
```

---

# 25. MVP完成条件

以下を満たしたらMVP完成とする。

1. MP3/WAVをCLIへ渡せる
2. `analysis.json` が生成される
3. `ai_prompt.md` が生成される
4. AIが生成した `song.yaml` を読み込める
5. Schema Validationが動作する
6. Rule Validationが動作する
7. Instrument Catalogから音色を選択できる
8. NoteEventをサンプル音源で再演奏できる
9. WAVを書き出せる
10. 原曲メロディをAIが変更しない構造になっている
11. AIなしでも手動YAMLで変換可能

---

# 26. MVPでやらないこと

以下は初期開発対象外。

- Web UI
- Vercel deployment
- MCP server
- OpenAI APIの直接統合
- 完全自動Agent
- 完全なSNES実機エミュレーション
- SPC完全互換
- 高精度な自動ミックス
- GUI YAML Editor
- ユーザー認証
- Cloud Storage

まずCoreを完成させる。

---

# 27. 実装優先順位

## Step 1

プロジェクト雛形。

```text
CLI
Pydantic Models
SongIR
Config Schema
```

---

## Step 2

Audio Loader。

```text
MP3/WAV
↓
WAV normalization
```

---

## Step 3

簡易Analyzer。

最初はすべての解析AI/MLモデルを揃えなくてもよい。

最低限。

```text
BPM
Pitch
MIDI
```

---

## Step 4

`analysis.json` と `ai_prompt.md` 生成。

この時点で人間 + ChatGPTとの手動ループを確認。

---

## Step 5

song.yaml Schema / Validator。

---

## Step 6

Instrument Catalog。

---

## Step 7

Renderer。

まずはNoteEventからSNES風Sampleを再生してWAV化。

---

## Step 8

Voice Allocation / Harmony Reduction。

---

## Step 9

Stem / Chord / Drum / Section解析精度向上。

---

## Step 10

SNES DSP再現。

```text
BRR
ADSR
Echo
FIR
```

---

# 28. Codexへの実装指示

この設計書を読んだCodexは、以下の方針で開発すること。

1. 最初から全機能を実装しない
2. MVPを小さい単位でコミット可能な状態にする
3. Converter CoreをAIサービスから独立させる
4. 型・Schemaを先に作る
5. 外部解析エンジンはAdapter化する
6. 不明な音楽解析結果を推測で補完しない
7. AIがNoteEventを書き換えられない設計にする
8. RendererはAI非依存とする
9. CLIから全Core機能を実行可能にする
10. テスト可能性を優先する

---

# 29. 最終ゴール

理想のUX。

```text
ユーザー:
「input/song.mp3 を原曲再現優先でSNES風にして」
```

Agent内部。

```text
analyze
↓
analysis.json
↓
AI arrangement planning
↓
song.yaml
↓
validate
↓
必要ならAI修正
↓
render
```

出力。

```text
output/song_snes.wav
```

将来的にはMCP経由でCodex / ChatGPT等から同一Coreを利用可能にする。

---

# 30. プロジェクトの一文定義

> **原曲の作曲情報を極力保存したまま、音源・Voice構成・DSP表現だけをSNES風へ写像する、AI補助型の半自動音楽変換パイプライン。**

